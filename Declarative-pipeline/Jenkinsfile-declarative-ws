pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Run build commands
                sh 'echo "Building project..."'
            }
        }
    }
}